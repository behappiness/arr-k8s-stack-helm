apiVersion: v1
kind: Pod
metadata:
  name: {{ include "flaresolverr.fullname" . }}-test-connection
  labels:
    {{- include "flaresolverr.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: test
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox:1.37
      command:
        - /bin/sh
        - -c
        - |
          set -e
          url="http://{{ include "flaresolverr.fullname" . }}:{{ .Values.service.port }}/health"
          echo "GET $url"
          wget -q -O- "$url" >/dev/null
          echo "flaresolverr answered /health"
      securityContext:
        allowPrivilegeEscalation: false
        runAsNonRoot: true
        runAsUser: 65534
        capabilities:
          drop:
            - ALL
        seccompProfile:
          type: RuntimeDefault
