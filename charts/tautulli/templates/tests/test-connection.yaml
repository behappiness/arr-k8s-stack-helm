apiVersion: v1
kind: Pod
metadata:
  name: {{ include "tautulli.fullname" . }}-test-connection
  labels:
    {{- include "tautulli.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: test
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox:1.37
      command:
        - /bin/sh
        - -c
        - |
          set -e
          url="http://{{ include "tautulli.fullname" . }}:{{ .Values.service.port }}/status"
          echo "GET $url"
          wget -q -O- "$url" >/dev/null
          echo "tautulli answered /status"
      securityContext:
        allowPrivilegeEscalation: false
        runAsNonRoot: true
        runAsUser: 65534
        capabilities:
          drop:
            - ALL
        seccompProfile:
          type: RuntimeDefault
