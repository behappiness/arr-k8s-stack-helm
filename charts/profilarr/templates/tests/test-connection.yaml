apiVersion: v1
kind: Pod
metadata:
  name: {{ include "profilarr.fullname" . }}-test-connection
  labels:
    {{- include "profilarr.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: test
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox:1.37
      command:
        - /bin/sh
        - -c
        - |
          set -e
          url="http://{{ include "profilarr.fullname" . }}:{{ .Values.service.port }}/"
          echo "GET $url"
          wget -q -O- "$url" >/dev/null
          echo "profilarr answered /"
      securityContext:
        allowPrivilegeEscalation: false
        runAsNonRoot: true
        runAsUser: 65534
        capabilities:
          drop:
            - ALL
        seccompProfile:
          type: RuntimeDefault
