apiVersion: v1
kind: Pod
metadata:
  name: {{ include "suggestarr.fullname" . }}-test-connection
  labels:
    {{- include "suggestarr.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: test
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox:1.37
      command:
        - /bin/sh
        - -c
        - |
          set -e
          url="http://{{ include "suggestarr.fullname" . }}:{{ .Values.service.port }}/"
          echo "GET $url"
          wget -q -O- "$url" >/dev/null
          echo "suggestarr served its web UI"
      securityContext:
        allowPrivilegeEscalation: false
        runAsNonRoot: true
        runAsUser: 65534
        capabilities:
          drop:
            - ALL
        seccompProfile:
          type: RuntimeDefault
