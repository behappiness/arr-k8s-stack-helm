apiVersion: v1
kind: Pod
metadata:
  name: {{ include "lidarr.fullname" . }}-test-connection
  labels:
    {{- include "lidarr.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: test
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox:1.37
      command:
        - /bin/sh
        - -c
        - |
          set -e
          url="http://{{ include "lidarr.fullname" . }}:{{ .Values.service.port }}{{ .Values.config.urlBase }}/ping"
          echo "GET $url"
          wget -q -O- "$url" | grep -q 'OK'
          echo "lidarr responded to /ping"
      securityContext:
        allowPrivilegeEscalation: false
        runAsNonRoot: true
        runAsUser: 65534
        capabilities:
          drop:
            - ALL
        seccompProfile:
          type: RuntimeDefault
